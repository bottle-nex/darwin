{
    "name": "@trymatcha/capsule-check",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsup src/index.ts --platform=node --format=esm",
        "test": "bun test src",
        "lint": "eslint . --max-warnings 0",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@types/node": "^25.9.3",
        "playwright": "^1.50.0"
    },
    "devDependencies": {
        "@trymatcha/eslint-config": "*",
        "@trymatcha/typescript-config": "*",
        "@types/bun": "latest",
        "tsup": "^8.0.0",
        "typescript": "^5"
    }
}
