import AgentMessage from "./AgentMessage";

/**
 * Upper section of card 04, "Gets better over time".
 *
 * Owns everything shown inside that card's media panel — replace the body with
 * whatever this card should illustrate.
 */
export default function ImprovesOverTimeMedia() {
    return <AgentMessage>{"Noted — I'll match your error-handling style."}</AgentMessage>;
}
