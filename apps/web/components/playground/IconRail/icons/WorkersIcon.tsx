import { Server, type LucideProps } from "lucide-react";

/** Glyph for the Workers surface in the icon rail. */
export default function WorkersIcon(props: LucideProps) {
    return <Server {...props} />;
}
