import { RiServerFill } from "react-icons/ri";
import type { IconBaseProps } from "react-icons";

/** Glyph for the Workers surface in the icon rail. */
export default function WorkersIcon(props: IconBaseProps) {
    return <RiServerFill {...props} />;
}
