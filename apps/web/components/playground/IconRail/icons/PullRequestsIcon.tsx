import { GitPullRequest, type LucideProps } from "lucide-react";

/** Glyph for the Pull Requests surface in the icon rail. */
export default function PullRequestsIcon(props: LucideProps) {
    return <GitPullRequest {...props} />;
}
