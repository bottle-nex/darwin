import { RiGitPullRequestFill } from "react-icons/ri";
import type { IconBaseProps } from "react-icons";

/** Glyph for the Pull Requests surface in the icon rail. */
export default function PullRequestsIcon(props: IconBaseProps) {
    return <RiGitPullRequestFill {...props} />;
}
